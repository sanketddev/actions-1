name: My First Workflow

on:
  push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkput@v6

        - name: List and Read files
          run: |
            echo "Welcome to Github actions"
            ls -ltra
            cat README.md